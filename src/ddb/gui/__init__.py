"""PySide6 GUI package.

Importing this module is cheap; it does NOT import PySide6 at top level
so the rest of the `ddb` package works in environments that don't have
Qt installed (CI, headless servers, packaging).
"""
